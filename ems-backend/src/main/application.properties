# Server settings
server.port=${PORT:8080}

# Primary: Construct URL from Railway's individual variables
# Fallback: Use MYSQL_PUBLIC_URL or direct Railway database URI
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://${MYSQLHOST:yamanote.proxy.rlwy.net}:${MYSQLPORT:19971}/${MYSQLDATABASE:railway}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}

spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:BfelrlAoArWZPxnKsJGrTvDFPWBjOKZK}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
#These two are used for server automatically changes the data
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true
